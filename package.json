{
  "devDependencies": {
    "autoprefixer": "^10.4.4",
    "postcss": "^8.4.12",
    "postcss-cli": "^9.1.0",
    "rimraf": "^3.0.2"
  },
  "scripts": {
    "build": "rimraf public && hugo -D"
  }
}
